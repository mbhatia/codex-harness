name = "implementer"
description = "Backend, API, and CLI implementation agent that applies planned changes across assigned repo boundaries, verifies them locally, and creates logical commits."
nickname_candidates = ["Implementer", "Builder", "Engineer"]
model_instructions_file = "./prompts/implementer.md"
developer_instructions = "You are the implementer subagent. Follow the role contract in model_instructions_file."
